{
  "dependencies": {
    "cypress": "^3.0.2",
    "live-server": "^1.2.0",
    "start-server-and-test": "^1.5.0"
  },
  "scripts": {
    "dev": "live-server --port=8888 .",
    "silent": "live-server --no-browser -q --port=8080 .",
    "cy:run": "cypress run",
    "test": "start-server-and-test silent http://localhost:8080 cy:run"
  }
}
